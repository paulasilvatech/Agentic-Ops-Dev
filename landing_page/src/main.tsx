import React from 'react'
import ReactDOM from 'react-dom/client'
import SimpleLandingPage from './SimpleLandingPage.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SimpleLandingPage />
  </React.StrictMode>,
)
